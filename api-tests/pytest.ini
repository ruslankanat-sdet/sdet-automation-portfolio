[pytest]
testpaths = tests
addopts = -ra -q --alluredir=allure-results
markers =
    smoke: fast guaranteed checks that gate the rest of the suite
    resilience: controlled fault-injection / resilience demonstrations
